import { FC } from 'react';
import { TMultiselectView } from '@/shared/types/Admin/shared/cells/Multiselect';

interface Props {
  data: TMultiselectView;
}

const View: FC<Props> = ({ data }) => {
  return <p>1</p>;
};

export { View };
